---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: identity-service
  ports:
  - name: grpc
    port: 50051
    targetPort: grpc
