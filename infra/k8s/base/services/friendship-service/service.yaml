---
apiVersion: v1
kind: Service
metadata:
  name: friendship-service
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: friendship-service
  ports:
  - name: grpc
    port: 50052
    targetPort: grpc
