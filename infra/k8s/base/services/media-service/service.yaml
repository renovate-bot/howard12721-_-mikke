---
apiVersion: v1
kind: Service
metadata:
  name: media-service
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: media-service
  ports:
  - name: grpc
    port: 50054
    targetPort: grpc
