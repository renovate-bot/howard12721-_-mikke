apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  ingressClassName: traefik
  rules:
    - host: api.mikke.uso.pw
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  name: http
